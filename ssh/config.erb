ControlMaster auto
ControlPath   <%= ENV['HOME'] %>/.ssh/tmp/%h_%p_%r
ControlPersist 4h

Host *
  ForwardAgent yes
  TCPKeepAlive no
  ServerAliveInterval 60
  ServerAliveCountMax 10
  GSSAPIAuthentication no
  